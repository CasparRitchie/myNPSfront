// function to see if a person is logged in as a user or admin

const LoggedInAs = () => {
    return (
        // add logic to decode the jwt and see the role from the encoded token
        <>
        "Admin or User?"
        </>
    )
} 
export default LoggedInAs